import React, { Component } from 'react';

class LoginComponent extends Component {
    render() {
        return <div>
            Connexion ou création de comptes (genre deux modes / onglets ? un peu dégueu les onglets peut-être)
        </div>
    }
}

export default LoginComponent